
android {
   signingConfigs {
       saroutyRelease {
           storeFile file('../android_release_keystore.jks')
           storePassword 'kamel5566'
           keyAlias 'sarouty'
           keyPassword 'kamel5566'
       }
       eSimsarRelease {
           storeFile file('../android_release_keystore.jks')
           storePassword 'kamel5566'
           keyAlias 'eSimsar'
           keyPassword 'kamel5566'
       }
       propertyFinderRelease {
           storeFile file('../android_release_keystore.jks')
           storePassword 'kamel5566'
           keyAlias 'propertyfinder'
           keyPassword 'kamel5566'
       }
       debug {
           storeFile file('../debug.keystore')
           storePassword 'android'
           keyAlias 'androiddebugkey'
           keyPassword 'android'
       }
       release {
           storeFile file('../android_release_keystore.jks')
           storePassword 'kamel5566'
           keyAlias 'propertyfinder'
           keyPassword 'kamel5566'
       }
   }
}
